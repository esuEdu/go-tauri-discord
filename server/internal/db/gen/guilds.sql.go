// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: guilds.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const addGuildMember = `-- name: AddGuildMember :one
INSERT INTO guild_members (guild_id, user_id, nickname)
VALUES ($1, $2, $3)
ON CONFLICT (guild_id, user_id) DO UPDATE SET nickname = EXCLUDED.nickname
RETURNING guild_id, user_id, nickname, joined_at
`

type AddGuildMemberParams struct {
	GuildID  uuid.UUID
	UserID   uuid.UUID
	Nickname *string
}

func (q *Queries) AddGuildMember(ctx context.Context, arg AddGuildMemberParams) (GuildMember, error) {
	row := q.db.QueryRow(ctx, addGuildMember, arg.GuildID, arg.UserID, arg.Nickname)
	var i GuildMember
	err := row.Scan(
		&i.GuildID,
		&i.UserID,
		&i.Nickname,
		&i.JoinedAt,
	)
	return i, err
}

const assignRole = `-- name: AssignRole :exec
INSERT INTO member_roles (guild_id, user_id, role_id)
VALUES ($1, $2, $3)
ON CONFLICT DO NOTHING
`

type AssignRoleParams struct {
	GuildID uuid.UUID
	UserID  uuid.UUID
	RoleID  uuid.UUID
}

func (q *Queries) AssignRole(ctx context.Context, arg AssignRoleParams) error {
	_, err := q.db.Exec(ctx, assignRole, arg.GuildID, arg.UserID, arg.RoleID)
	return err
}

const createChannel = `-- name: CreateChannel :one
INSERT INTO channels (id, guild_id, parent_id, kind, name, topic, position)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, guild_id, parent_id, kind, name, topic, position, created_at
`

type CreateChannelParams struct {
	ID       uuid.UUID
	GuildID  uuid.UUID
	ParentID *uuid.UUID
	Kind     string
	Name     string
	Topic    *string
	Position int32
}

func (q *Queries) CreateChannel(ctx context.Context, arg CreateChannelParams) (Channel, error) {
	row := q.db.QueryRow(ctx, createChannel,
		arg.ID,
		arg.GuildID,
		arg.ParentID,
		arg.Kind,
		arg.Name,
		arg.Topic,
		arg.Position,
	)
	var i Channel
	err := row.Scan(
		&i.ID,
		&i.GuildID,
		&i.ParentID,
		&i.Kind,
		&i.Name,
		&i.Topic,
		&i.Position,
		&i.CreatedAt,
	)
	return i, err
}

const createGuild = `-- name: CreateGuild :one
INSERT INTO guilds (id, name, owner_id)
VALUES ($1, $2, $3)
RETURNING id, name, owner_id, icon_key, created_at
`

type CreateGuildParams struct {
	ID      uuid.UUID
	Name    string
	OwnerID uuid.UUID
}

func (q *Queries) CreateGuild(ctx context.Context, arg CreateGuildParams) (Guild, error) {
	row := q.db.QueryRow(ctx, createGuild, arg.ID, arg.Name, arg.OwnerID)
	var i Guild
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.OwnerID,
		&i.IconKey,
		&i.CreatedAt,
	)
	return i, err
}

const createRole = `-- name: CreateRole :one
INSERT INTO roles (id, guild_id, name, permissions, position, is_default)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, guild_id, name, permissions, position, is_default
`

type CreateRoleParams struct {
	ID          uuid.UUID
	GuildID     uuid.UUID
	Name        string
	Permissions int64
	Position    int32
	IsDefault   bool
}

func (q *Queries) CreateRole(ctx context.Context, arg CreateRoleParams) (Role, error) {
	row := q.db.QueryRow(ctx, createRole,
		arg.ID,
		arg.GuildID,
		arg.Name,
		arg.Permissions,
		arg.Position,
		arg.IsDefault,
	)
	var i Role
	err := row.Scan(
		&i.ID,
		&i.GuildID,
		&i.Name,
		&i.Permissions,
		&i.Position,
		&i.IsDefault,
	)
	return i, err
}

const deleteChannel = `-- name: DeleteChannel :exec
DELETE FROM channels WHERE id = $1
`

func (q *Queries) DeleteChannel(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteChannel, id)
	return err
}

const deleteGuild = `-- name: DeleteGuild :exec
DELETE FROM guilds WHERE id = $1
`

func (q *Queries) DeleteGuild(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteGuild, id)
	return err
}

const getChannel = `-- name: GetChannel :one
SELECT id, guild_id, parent_id, kind, name, topic, position, created_at FROM channels WHERE id = $1
`

func (q *Queries) GetChannel(ctx context.Context, id uuid.UUID) (Channel, error) {
	row := q.db.QueryRow(ctx, getChannel, id)
	var i Channel
	err := row.Scan(
		&i.ID,
		&i.GuildID,
		&i.ParentID,
		&i.Kind,
		&i.Name,
		&i.Topic,
		&i.Position,
		&i.CreatedAt,
	)
	return i, err
}

const getGuild = `-- name: GetGuild :one
SELECT id, name, owner_id, icon_key, created_at FROM guilds WHERE id = $1
`

func (q *Queries) GetGuild(ctx context.Context, id uuid.UUID) (Guild, error) {
	row := q.db.QueryRow(ctx, getGuild, id)
	var i Guild
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.OwnerID,
		&i.IconKey,
		&i.CreatedAt,
	)
	return i, err
}

const getGuildMember = `-- name: GetGuildMember :one
SELECT guild_id, user_id, nickname, joined_at FROM guild_members WHERE guild_id = $1 AND user_id = $2
`

type GetGuildMemberParams struct {
	GuildID uuid.UUID
	UserID  uuid.UUID
}

func (q *Queries) GetGuildMember(ctx context.Context, arg GetGuildMemberParams) (GuildMember, error) {
	row := q.db.QueryRow(ctx, getGuildMember, arg.GuildID, arg.UserID)
	var i GuildMember
	err := row.Scan(
		&i.GuildID,
		&i.UserID,
		&i.Nickname,
		&i.JoinedAt,
	)
	return i, err
}

const listChannels = `-- name: ListChannels :many
SELECT id, guild_id, parent_id, kind, name, topic, position, created_at FROM channels WHERE guild_id = $1 ORDER BY position, id
`

func (q *Queries) ListChannels(ctx context.Context, guildID uuid.UUID) ([]Channel, error) {
	rows, err := q.db.Query(ctx, listChannels, guildID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Channel{}
	for rows.Next() {
		var i Channel
		if err := rows.Scan(
			&i.ID,
			&i.GuildID,
			&i.ParentID,
			&i.Kind,
			&i.Name,
			&i.Topic,
			&i.Position,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGuildMemberIDs = `-- name: ListGuildMemberIDs :many
SELECT user_id FROM guild_members WHERE guild_id = $1
`

func (q *Queries) ListGuildMemberIDs(ctx context.Context, guildID uuid.UUID) ([]uuid.UUID, error) {
	rows, err := q.db.Query(ctx, listGuildMemberIDs, guildID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []uuid.UUID{}
	for rows.Next() {
		var user_id uuid.UUID
		if err := rows.Scan(&user_id); err != nil {
			return nil, err
		}
		items = append(items, user_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGuildMembers = `-- name: ListGuildMembers :many
SELECT m.guild_id, m.user_id, m.nickname, m.joined_at, u.username, u.avatar_key
FROM guild_members m
JOIN users u ON u.id = m.user_id
WHERE m.guild_id = $1
ORDER BY u.username
`

type ListGuildMembersRow struct {
	GuildID   uuid.UUID
	UserID    uuid.UUID
	Nickname  *string
	JoinedAt  time.Time
	Username  string
	AvatarKey *string
}

func (q *Queries) ListGuildMembers(ctx context.Context, guildID uuid.UUID) ([]ListGuildMembersRow, error) {
	rows, err := q.db.Query(ctx, listGuildMembers, guildID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListGuildMembersRow{}
	for rows.Next() {
		var i ListGuildMembersRow
		if err := rows.Scan(
			&i.GuildID,
			&i.UserID,
			&i.Nickname,
			&i.JoinedAt,
			&i.Username,
			&i.AvatarKey,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGuildOverwrites = `-- name: ListGuildOverwrites :many
SELECT o.channel_id, o.target_id, o.target_type, o.allow, o.deny FROM channel_overwrites o
JOIN channels c ON c.id = o.channel_id
WHERE c.guild_id = $1
`

func (q *Queries) ListGuildOverwrites(ctx context.Context, guildID uuid.UUID) ([]ChannelOverwrite, error) {
	rows, err := q.db.Query(ctx, listGuildOverwrites, guildID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ChannelOverwrite{}
	for rows.Next() {
		var i ChannelOverwrite
		if err := rows.Scan(
			&i.ChannelID,
			&i.TargetID,
			&i.TargetType,
			&i.Allow,
			&i.Deny,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGuildsForUser = `-- name: ListGuildsForUser :many
SELECT g.id, g.name, g.owner_id, g.icon_key, g.created_at FROM guilds g
JOIN guild_members m ON m.guild_id = g.id
WHERE m.user_id = $1
ORDER BY g.created_at
`

func (q *Queries) ListGuildsForUser(ctx context.Context, userID uuid.UUID) ([]Guild, error) {
	rows, err := q.db.Query(ctx, listGuildsForUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Guild{}
	for rows.Next() {
		var i Guild
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.OwnerID,
			&i.IconKey,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGuildsOwnedBy = `-- name: ListGuildsOwnedBy :many
SELECT id, name, owner_id, icon_key, created_at FROM guilds WHERE owner_id = $1
`

func (q *Queries) ListGuildsOwnedBy(ctx context.Context, ownerID uuid.UUID) ([]Guild, error) {
	rows, err := q.db.Query(ctx, listGuildsOwnedBy, ownerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Guild{}
	for rows.Next() {
		var i Guild
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.OwnerID,
			&i.IconKey,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRoles = `-- name: ListRoles :many
SELECT id, guild_id, name, permissions, position, is_default FROM roles WHERE guild_id = $1 ORDER BY position DESC, id
`

func (q *Queries) ListRoles(ctx context.Context, guildID uuid.UUID) ([]Role, error) {
	rows, err := q.db.Query(ctx, listRoles, guildID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Role{}
	for rows.Next() {
		var i Role
		if err := rows.Scan(
			&i.ID,
			&i.GuildID,
			&i.Name,
			&i.Permissions,
			&i.Position,
			&i.IsDefault,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const nextGuildOwner = `-- name: NextGuildOwner :one
SELECT m.user_id
FROM guild_members m
LEFT JOIN member_roles mr ON mr.guild_id = m.guild_id AND mr.user_id = m.user_id
LEFT JOIN roles r ON r.id = mr.role_id
WHERE m.guild_id = $1 AND m.user_id <> $2
GROUP BY m.user_id, m.joined_at
ORDER BY COALESCE(MAX(r.position), -1) DESC, m.joined_at, m.user_id
LIMIT 1
`

type NextGuildOwnerParams struct {
	GuildID   uuid.UUID
	LeavingID uuid.UUID
}

func (q *Queries) NextGuildOwner(ctx context.Context, arg NextGuildOwnerParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, nextGuildOwner, arg.GuildID, arg.LeavingID)
	var user_id uuid.UUID
	err := row.Scan(&user_id)
	return user_id, err
}

const removeGuildMember = `-- name: RemoveGuildMember :exec
DELETE FROM guild_members WHERE guild_id = $1 AND user_id = $2
`

type RemoveGuildMemberParams struct {
	GuildID uuid.UUID
	UserID  uuid.UUID
}

func (q *Queries) RemoveGuildMember(ctx context.Context, arg RemoveGuildMemberParams) error {
	_, err := q.db.Exec(ctx, removeGuildMember, arg.GuildID, arg.UserID)
	return err
}

const resolveChannelAccess = `-- name: ResolveChannelAccess :one
SELECT
    c.id, c.guild_id, c.parent_id, c.kind, c.name, c.topic, c.position, c.created_at,
    g.owner_id,
    (m.user_id IS NOT NULL)::bool AS is_member,
    COALESCE((
        SELECT json_agg(json_build_object('id', r.id, 'permissions', r.permissions)
                        ORDER BY r.position DESC, r.id)
        FROM roles r
        WHERE r.guild_id = c.guild_id
          AND (
            r.is_default
            OR EXISTS (
              SELECT 1 FROM member_roles mr
              WHERE mr.role_id = r.id AND mr.user_id = $1
            )
          )
    ), '[]'::json)::text AS roles,
    COALESCE((
        SELECT json_agg(json_build_object('target_id', o.target_id,
                                          'target_type', o.target_type,
                                          'allow', o.allow,
                                          'deny', o.deny))
        FROM channel_overwrites o
        WHERE o.channel_id = c.id
    ), '[]'::json)::text AS overwrites
FROM channels c
JOIN guilds g ON g.id = c.guild_id
LEFT JOIN guild_members m ON m.guild_id = c.guild_id AND m.user_id = $1
WHERE c.id = $2
`

type ResolveChannelAccessParams struct {
	UserID    uuid.UUID
	ChannelID uuid.UUID
}

type ResolveChannelAccessRow struct {
	Channel    Channel
	OwnerID    uuid.UUID
	IsMember   bool
	Roles      string
	Overwrites string
}

func (q *Queries) ResolveChannelAccess(ctx context.Context, arg ResolveChannelAccessParams) (ResolveChannelAccessRow, error) {
	row := q.db.QueryRow(ctx, resolveChannelAccess, arg.UserID, arg.ChannelID)
	var i ResolveChannelAccessRow
	err := row.Scan(
		&i.Channel.ID,
		&i.Channel.GuildID,
		&i.Channel.ParentID,
		&i.Channel.Kind,
		&i.Channel.Name,
		&i.Channel.Topic,
		&i.Channel.Position,
		&i.Channel.CreatedAt,
		&i.OwnerID,
		&i.IsMember,
		&i.Roles,
		&i.Overwrites,
	)
	return i, err
}

const resolveGuildAccess = `-- name: ResolveGuildAccess :one
SELECT
    g.owner_id,
    (m.user_id IS NOT NULL)::bool AS is_member,
    COALESCE((
        SELECT json_agg(json_build_object('id', r.id, 'permissions', r.permissions)
                        ORDER BY r.position DESC, r.id)
        FROM roles r
        WHERE r.guild_id = g.id
          AND (
            r.is_default
            OR EXISTS (
              SELECT 1 FROM member_roles mr
              WHERE mr.role_id = r.id AND mr.user_id = $1
            )
          )
    ), '[]'::json)::text AS roles
FROM guilds g
LEFT JOIN guild_members m ON m.guild_id = g.id AND m.user_id = $1
WHERE g.id = $2
`

type ResolveGuildAccessParams struct {
	UserID  uuid.UUID
	GuildID uuid.UUID
}

type ResolveGuildAccessRow struct {
	OwnerID  uuid.UUID
	IsMember bool
	Roles    string
}

func (q *Queries) ResolveGuildAccess(ctx context.Context, arg ResolveGuildAccessParams) (ResolveGuildAccessRow, error) {
	row := q.db.QueryRow(ctx, resolveGuildAccess, arg.UserID, arg.GuildID)
	var i ResolveGuildAccessRow
	err := row.Scan(&i.OwnerID, &i.IsMember, &i.Roles)
	return i, err
}

const transferGuildOwnership = `-- name: TransferGuildOwnership :exec
UPDATE guilds SET owner_id = $1 WHERE id = $2
`

type TransferGuildOwnershipParams struct {
	OwnerID uuid.UUID
	ID      uuid.UUID
}

func (q *Queries) TransferGuildOwnership(ctx context.Context, arg TransferGuildOwnershipParams) error {
	_, err := q.db.Exec(ctx, transferGuildOwnership, arg.OwnerID, arg.ID)
	return err
}

const upsertChannelOverwrite = `-- name: UpsertChannelOverwrite :exec
INSERT INTO channel_overwrites (channel_id, target_id, target_type, allow, deny)
VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (channel_id, target_id)
DO UPDATE SET allow = EXCLUDED.allow, deny = EXCLUDED.deny
`

type UpsertChannelOverwriteParams struct {
	ChannelID  uuid.UUID
	TargetID   uuid.UUID
	TargetType string
	Allow      int64
	Deny       int64
}

func (q *Queries) UpsertChannelOverwrite(ctx context.Context, arg UpsertChannelOverwriteParams) error {
	_, err := q.db.Exec(ctx, upsertChannelOverwrite,
		arg.ChannelID,
		arg.TargetID,
		arg.TargetType,
		arg.Allow,
		arg.Deny,
	)
	return err
}
